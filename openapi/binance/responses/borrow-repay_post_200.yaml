description: Margin account borrow/repay
content:
  application/json:
    schema:
      type: object
      properties:
        tranId:
          type: integer
          format: int64
          example: 100000001
      required:
      - tranId
