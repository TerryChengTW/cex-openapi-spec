name: pendingType
in: query
required: true
description: 'Supported values: Order Types Note that MARKET orders using quoteOrderQty
  are not supported.'
schema:
  type: string
  enum:
  - LIMIT
  - MARKET
  - STOP_LOSS
  - STOP_LOSS_LIMIT
  - TAKE_PROFIT
  - TAKE_PROFIT_LIMIT
  - LIMIT_MAKER
