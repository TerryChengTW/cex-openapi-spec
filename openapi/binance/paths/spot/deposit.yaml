paths:
  /sapi/v1/nft/history/deposit:
    get:
      summary: Get NFT Deposit History(USER_DATA)
      description: '- The max interval between startTime and endTime is 90 days.

        - If startTime and endTime are not sent, the recent 7 days'' data will be
        returned.


        Weight(UID): 3000'
      tags:
      - NFT
      parameters:
      - $ref: ..\..\parameters/startTime.yaml
      - $ref: ..\..\parameters/endTime.yaml
      - $ref: ..\..\parameters/limit50.yaml
      - $ref: ..\..\parameters/page.yaml
      - $ref: ..\..\parameters/recvWindow.yaml
      - $ref: ..\..\parameters/timestamp.yaml
      - $ref: ..\..\parameters/signature.yaml
      security:
      - ApiKeyAuth: []
      responses:
        '200':
          $ref: ..\..\responses/deposit_get_200.yaml
        '400':
          $ref: ..\..\responses/deposit_get_400.yaml
        '401':
          $ref: ..\..\responses/deposit_get_401.yaml
