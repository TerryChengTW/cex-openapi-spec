type: object
properties:
  symbol:
    type: string
  priceChange:
    type: string
    description: Absolute price change
  priceChangePercent:
    type: string
    description: Relative price change in percent
  weightedAvgPrice:
    type: string
    description: QuoteVolume / Volume
  openPrice:
    type: string
  highPrice:
    type: string
  lowPrice:
    type: string
  lastPrice:
    type: string
  volume:
    type: string
    description: Volume in base asset
  quoteVolume:
    type: string
    description: Sum of (price * volume) for all trades
  openTime:
    type: integer
    description: Open time for ticker window
  closeTime:
    type: integer
    description: Close time for ticker window
  firstId:
    type: integer
    description: Trade ID of the first trade in the interval
  lastId:
    type: integer
    description: Trade ID of the last trade in the interval
  count:
    type: integer
    description: Number of trades in the interval
required:
  - symbol
  - openPrice
  - highPrice
  - lowPrice
  - lastPrice
  - volume
  - quoteVolume
  - openTime
  - closeTime
  - firstId
  - lastId
  - count
