paths:
  /api/v3/time:
    get:
      tags:
        - BinanceSpotGeneral
      summary: Check server time
      description: Test connectivity to the Rest API and get the current server time.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: './schemas/ServerTimeResponse.yaml#/ServerTimeResponse'
              example:
                serverTime: 1499827319559
